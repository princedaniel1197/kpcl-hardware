# api — FastAPI and WebSocket (Stage 12)

REST endpoints for tags, trends, asset hierarchy, KPI values and event frames, plus
a WebSocket streaming the collector event stream to the browser.

FastAPI is the only web framework in this project.

Empty until Stage 12.
