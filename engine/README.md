# engine — asset model, quality, KPIs, event frames (Stages 5-8)

The tag and element naming convention (ISA-95 / ISA-88) is documented here at
Stage 5 and is not changed ad hoc. (§385)

A calculation with a Bad input returns Bad, with a reason naming the offending
input. Not zero, not the last good value, not an interpolation. (§318)

Empty until Stage 5.
