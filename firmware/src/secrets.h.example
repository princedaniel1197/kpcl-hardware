// Copy to secrets.h and fill in. secrets.h is in .gitignore and must never be
// committed: this repository is public.
#pragma once

static const char *WIFI_SSID     = "your-network";
static const char *WIFI_PASSWORD = "your-password";
